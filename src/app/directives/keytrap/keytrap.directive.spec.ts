import { KeytrapDirective } from './keytrap.directive';

describe('KeytrapDirective', () => {
  it('should create an instance', () => {
    const directive = new KeytrapDirective();
    expect(directive).toBeTruthy();
  });
});
